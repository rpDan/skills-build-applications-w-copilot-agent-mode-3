{
  "name": "octofit-tracker",
  "private": true,
  "scripts": {
    "dev": "npm --prefix octofit-tracker/frontend run dev -- --host 0.0.0.0",
    "build": "npm --prefix octofit-tracker/frontend run build",
    "lint": "npm --prefix octofit-tracker/frontend run lint"
  }
}
